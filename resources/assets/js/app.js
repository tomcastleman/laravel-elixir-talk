// Load jquery and underscore in to global scope
window.$ = window.jQuery = require('jquery');
window._ = require('underscore');

// Load bootstrap
require('bootstrap-sass');

// Load Vue components
require('./vue');
